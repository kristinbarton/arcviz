[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "arcviz"
version = "0.1.0"
dependencies = [
    "xarray",
    "pandas",
    "numpy",
    "matplotlib",
    "scipy"
]

[tool.setuptools.packages.find]
where = ["src"]
